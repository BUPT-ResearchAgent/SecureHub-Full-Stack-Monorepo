# 5-C 交付报告：C 知识数据、质量闸门与演示验收全阶段收敛

## 1. 基线

- 起始 commit：`4afe4f52825dc56b935576053d753be4a86b8a89`
- 当前分支：`dev`
- 工作树状态：本轮 C 文件有未提交修改；开工前已有 `.codegraph/daemon.pid` 与 `data/storage/`，未清理。
- 本轮前 C 阶段判断：P0 evidence/RAG/no-evidence 已有基础；P0.5 live LLM 门禁已搭建但默认 skip；P1 只有 GitHub Docs / 官方资料 smoke，MediaCrawler/MindSpider 不能按生产能力宣称完成；A 的 Harness + SSE 主路径仍是全局瓶颈，C 不越界修改。

## 2. 子智能体使用情况

- 是否使用子智能体：是
- 子智能体角色：`securehub_backend_rag_worker` 等价 C-knowledge-worker
- 子智能体负责范围：C 边界内的 seed、loader/crawler fixture、RAG/no-evidence/live LLM 测试、demo 文档、`demo_smoke.ps1` 和本报告。
- 主 Codex 最终验收范围：审查 diff、边界自检、重跑必需测试、修正报告结构与风险表述。

## 3. 阶段完成度

| 阶段 | 目标 | 本轮结果 | 证据文件 | 状态 |
| --- | --- | --- | --- | --- |
| P0 | evidence/RAG/no-evidence/seed | WebSec seed 幂等，覆盖 17 nodes、68 chunks、5 SQL 注入题目；RAG smoke 扩到 CSRF、SSRF、访问控制、命令执行等；metadata 缺 `source_url` / `rights_note` 不进入 done；SSRF seed 增加 `platform=mineru`、`asset_type=pdf`、`page_no=12` 的离线 evidence。 | `backend/app/db/seeds/seed_course_websec.py`; `backend/tests/rag/test_retrieve_course_websec.py`; `backend/tests/hallucination/test_no_evidence_queries.py` | 完成 |
| P0.5 | live LLM 门禁 | 默认 skip；只有 `ENABLE_LLM_LIVE_TESTS=true` 且存在 DeepSeek 或讯飞 key 才允许真实调用；成功路径断言 evidence/source_url/agent_runs/generated_resources/provider/model/token/cost；无 key 不宣称通过。 | `backend/tests/llm_live/test_p0_real_llm.py`; `docs/demo/llm_live_acceptance.md`; `.github/workflows/ci.yml` | 完成 |
| P1 | 多源采集/官方资料/MediaCrawler fixture | GitHub Docs、OWASP/PortSwigger Scrapling fixture、B 站 MediaCrawler export fixture 均离线可测并写入统一表；MindSpider 仅保留 reference 边界，不接生产链路。 | `backend/tests/knowledge/test_github_docs_loader.py`; `backend/tests/knowledge/test_scrapling_public_loader.py`; `backend/tests/knowledge/test_mediacrawler_normalizer.py`; `docs/demo/websec_source_inventory.md` | 部分完成 |
| P1.5 | demo_smoke/实际应用验收 | `demo_smoke.ps1` 覆盖 health、llm/health、courses、rag/search、course plan、resources/generate、agent_runs，输出 PASS/FAIL 与 Owner；真实主链路质量仍依赖 A/B endpoint 与前端联调。 | `scripts/demo_smoke.ps1`; `docs/demo/seven-minute-demo-checklist.md` | 部分完成 |
| P2 | source rights/final checklist/reference data | source inventory 更新到 source_url/rights_note/collection_mode/asset_type/入库/测试状态；新增 source rights note；7 分钟 checklist 只陈述 C 负责状态。 | `docs/demo/websec_source_inventory.md`; `docs/demo/source-rights-note.md`; `docs/demo/seven-minute-demo-checklist.md` | 完成 |

## 4. 代码与文档变更

- 文件：`backend/app/db/seeds/seed_course_websec.py`、`backend/app/services/knowledge/crawling/media_source_normalizer.py`、`backend/tests/rag/test_retrieve_course_websec.py`、`backend/tests/hallucination/test_no_evidence_queries.py`、`backend/tests/knowledge/test_mediacrawler_normalizer.py`、`backend/tests/llm_live/test_p0_real_llm.py`、`docs/demo/*.md`、`scripts/demo_smoke.ps1`、`Workout/5-C.md`
- 目的：把 C 的 seed/RAG/no-evidence/live-test/demo-smoke/source-rights 状态变成可重复离线验收。
- 风险：MediaCrawler `collection_mode` 从 `mediacrawler_export` 对齐为 `mediacrawler`，若旧前端 mock 仍依赖旧枚举需 B 确认；`demo_smoke.ps1` 仍会暴露 A/B endpoint 未 ready 的真实失败。

## 5. 测试结果

```powershell
cd backend
uv run pytest tests/rag/test_retrieve_course_websec.py tests/hallucination/test_no_evidence_queries.py tests/knowledge/test_github_docs_loader.py -q -m "not llm_live"
```

主 Codex 最终验收结果：`12 passed in 3.18s`。

```powershell
cd backend
uv run pytest -m "not llm_live" -q
```

主 Codex 最终验收结果：`110 passed, 2 deselected in 34.79s`。

```powershell
cd backend
uv run pytest tests/llm_live/test_p0_real_llm.py -q
```

主 Codex 最终验收结果：`2 skipped in 0.03s`。这是预期结果：未设置 `ENABLE_LLM_LIVE_TESTS=true`，未消耗 API。

```powershell
.\scripts\demo_smoke.ps1
```

主 Codex 最终验收结果：`PASSED: 7 step(s)`。步骤：health、llm/health、courses、rag/search、course plan、resources/generate、agent_runs。`rag/search` 输出 `fallback=True`，`agent_runs` 输出 `items=0`；该结果是 fixture/smoke 验收，不代表真实 LLM 已跑通。

## 6. 未完成与依赖

- A 边界：真实 LLM provider、Harness 主链路、SSE 主路径、agent_runs 真实质量仍由 A 负责。
- B 边界：EvidenceDrawer / AgentTracePanel / PresenterMode 的真实 UI 联调仍由 B 负责。
- C 边界：真实 DeepSeek / 讯飞调用未运行，因为没有明确 API 消耗许可和真实 key；状态为“门禁完成，真实调用待有 key 后验证”。

## 7. 回滚预案

- seed/RAG 回滚：回滚 `backend/app/db/seeds/seed_course_websec.py` 与 `backend/tests/rag/test_retrieve_course_websec.py` 的本轮改动，保留 P0 已通过基础链路。
- MediaCrawler 回滚：回滚 `backend/app/services/knowledge/crawling/media_source_normalizer.py` 和 `backend/tests/knowledge/test_mediacrawler_normalizer.py`。
- live LLM 回滚：确认 `.github/workflows/ci.yml` 仍执行 `uv run pytest -m "not llm_live" -q`；若误入 CI，立即恢复默认 skip。
- smoke/docs 回滚：`scripts/demo_smoke.ps1`、`docs/demo/` 和 `Workout/5-C.md` 可独立文件级回滚。

## 8. 下一步建议

1. A 先完成真实 provider + Harness 主路径，保证资源生成真实写入 `agent_runs/generated_resources`。
2. B 用本轮 evidence 字段联调 EvidenceDrawer / AgentTracePanel，不再依赖静态 mock。
3. 项目负责人提供一次明确 API 预算窗口后，手动跑 DeepSeek 或讯飞 `llm_live` 门禁并记录真实结果。
